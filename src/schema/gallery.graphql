type Image{
  source: String!
  thumb: String!
}

type BlogPost{
  id: String
  body: String
}

type Query {
  helloWorld: String!
  images: [Image]
  posts: [BlogPost]
}

query getAllImages{
  images{
    source,
    thumb,
  }
}

query getAllBlogPosts{
  posts{
    id,
    body,
  }
}

